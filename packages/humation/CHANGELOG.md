## 0.1.0

- Initial release. Pure-Dart Humation engine: manifest model, FNV-1a selection
  (UTF-16, web-safe), `createAvatar` with SVG string and structured render data,
  manifest validator, and UI helpers. Byte-identical selection to the reference
  engine.
